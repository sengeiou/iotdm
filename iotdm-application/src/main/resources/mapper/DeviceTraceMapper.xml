<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aibaixun.iotdm.mapper.DeviceTraceMapper">


    <insert id="updateDeviceDebug" >
        INSERT INTO t_device_trace ( device_id, trace_debug, creator, tenant_id )
        VALUES
            ( #{deviceId}, #{traceDebug}, #{userId}, #{tenantId} )
            ON DUPLICATE KEY UPDATE trace_debug = trace_debug ^1
    </insert>
</mapper>
